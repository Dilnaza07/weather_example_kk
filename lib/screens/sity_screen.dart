import 'package:flutter/material.dart';

class SityScreen extends StatelessWidget {
  const SityScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
